% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline.R
\name{pumf_locate_or_download}
\alias{pumf_locate_or_download}
\title{Locate or download a PUMF version directory}
\usage{
pumf_locate_or_download(
  series,
  version,
  cache_path = getOption("canpumf.cache_path", tempdir()),
  refresh = FALSE,
  redownload = FALSE
)
}
\arguments{
\item{series}{Survey series acronym, e.g. `"SFS"`.}

\item{version}{Version string, e.g. `"2019"` or `"2021 (individuals)"`.}

\item{cache_path}{Root cache directory. Defaults to
`getOption("canpumf.cache_path", tempdir())`.}

\item{refresh}{If `TRUE`, delete the `.duckdb` file and `metadata/` dir so
the downstream stages re-run. Raw zip and extracted files are untouched.}
}
\value{
The version directory path, invisibly.
}
\description{
Ensures the version directory exists and its zip has been extracted.
With `refresh = TRUE`, clears the DuckDB file(s) and `metadata/` subdirectory
so that Stages 2 and 3 re-run, but does **not** re-download or re-extract
raw data.
}
\details{
For EFT-only surveys (older Census years), stops with an informative error
asking the user to deposit the zip manually.
}
\keyword{internal}
