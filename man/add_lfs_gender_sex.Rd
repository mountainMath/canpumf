% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lfs_helpers.R
\name{add_lfs_gender_sex}
\alias{add_lfs_gender_sex}
\title{Add a harmonised GENDER_SEX column to an LFS table}
\usage{
add_lfs_gender_sex(tbl)
}
\arguments{
\item{tbl}{A lazy `dplyr::tbl()` returned by [get_pumf()] for an LFS
survey.  Must contain at least one of `SEX` or `GENDER`.}
}
\value{
The same lazy table with a new `GENDER_SEX` column positioned
  after `GENDER` (if present) or after `SEX`.
}
\description{
LFS introduced `GENDER` (with values `"Men+"` / `"Women+"` / `"Non-binary
persons"`) to replace the binary `SEX` variable (`"Male"` / `"Female"`)
starting in 2020.  In any given row exactly one of the two columns is
non-`NA`.  `add_lfs_gender_sex()` coalesces them into a single `GENDER_SEX`
column, recoding `SEX` values to the `GENDER` scale so the result is
consistent across all LFS vintages.
}
\details{
The mapping applied to `SEX` when `GENDER` is `NA`:
\itemize{
  \item `"Male"`   → `"Men+"`
  \item `"Female"` → `"Women+"`
}

`GENDER_SEX` is inserted after `GENDER` when that column is present, or
after `SEX` otherwise.
}
\examples{
\dontrun{
lfs <- get_pumf("LFS")
lfs |> add_lfs_gender_sex() |>
  dplyr::count(SEX, GENDER, GENDER_SEX) |> dplyr::collect()
close_pumf(lfs)
}
}
\seealso{
[get_pumf()], [add_lfs_date()]
}
