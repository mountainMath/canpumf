# Check whether canonical metadata exists for a version directory

Check whether canonical metadata exists for a version directory

## Usage

``` r
metadata_exists(version_dir)
```

## Arguments

- version_dir:

  Path to the version directory.

## Value

Logical.
