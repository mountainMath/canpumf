# Parse PUMF medatadata

Parse PUMF medatadata

## Usage

``` r
parse_pumf_metadata_cards(pumf_base_path, layout_mask = NULL)
```

## Arguments

- pumf_base_path:

  path for pumf data

- layout_mask:

  optional parameter to identify layour files if there are many
